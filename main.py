# AREA OF A SQUARE

# Get the length of the user
length = float(input("Enter the length of the square: "))
# print(length)

# Caculate and store the area in a variable (area = length * length)
area = length * length

# Print the calculated area to the terminal
print(f"The calculated area is: {area}")